import 'package:flutter/material.dart';

class PasswordTerminal extends StatelessWidget {
  const PasswordTerminal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
